--- a/chadwm/dwm.c
+++ b/chadwm/dwm.c
@@ -2225,12 +2225,12 @@
 recttomon(int x, int y, int w, int h) {
 	Monitor *m, *r = selmon;
 	int a, area = 0;
-
 	for (m = mons; m; m = m->next)
 		if ((a = INTERSECT(x, y, w, h, m)) > area) {
 			area = a;
 			r = m;
 		}
+	if (area == 0)
+		return selmon;
 	return r;
 }
 EOF